import { Text } from '@react-three/drei'

export default function ProjectsSection() {
    return (
        <group position={[0, 0, -8]}>
        <Text
        fontSize={1}
        color="white"
        anchorX="center"
        anchorY="middle"
    >
        PROJECTS
        </Text>
        </group>
    )
}
